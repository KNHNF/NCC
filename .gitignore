# data lives in the sibling ncc-challenge/ clone, not duplicated here
data/
*.npy
*.pth
# exception: the checkpoint used for the reported results, kept in the repo
# so evaluate.py is runnable without retraining first (~57MB, within GitHub's limit)
!output/best_model.pth

# python
__pycache__/
*.pyc
.venv/
venv/

# secrets
.env
*.env
GEMINI_API_KEY*

# os
.DS_Store
Thumbs.db

# kaggle run outputs, large checkpoints and archives, keep local only
# (output/best_model.pth is the one exception, whitelisted above)
best_model_scale.pth
*.zip
output/new output/
output/__huggingface_repos__.json
output/__results___files/

# superseded by the pass/fail pair
output/figures/pipeline_demo.png

# scratch working notes and personal messages, not repo documentation
TEAM-NEXT-STEPS.md
akilesh-update-and-ask.md

# demo source images copied out of the challenge dataset for recording
demo-images/

# gradio runtime cache
.gradio/
